export type avaliationRate = {
    feedbackRate: string,
    total: number
    userId: string
}

export type user = {
    name: string,
    password: string,
    segment: string
    employeeName?: string
}